[tox]
envlist = py310, py311, py312, py313, flake8

[testenv:flake8]
basepython = python
deps = flake8
commands = flake8 justice tests

[testenv]
setenv =
    PYTHONPATH = {toxinidir}

commands = python -m unittest discover -s tests -p "*.py"
